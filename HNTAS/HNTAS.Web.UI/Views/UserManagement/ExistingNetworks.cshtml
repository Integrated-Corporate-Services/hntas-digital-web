@using HNTAS.Api.Client.Model
@model ExistingNetworkResponse

@{
    ViewData["Title"] = "Heat networks";
    var currentSort = ViewBag.CurrentSort as string;
    var currentOrder = ViewBag.CurrentOrder as string;
    var currentPage = (int)(ViewBag.CurrentPage);
    var pageSize = (int)(ViewBag.PageSize);
    var totalPages = (int)(ViewBag.TotalPages);
    var userId = Model.UserId;

    string GetSortDirection()
    {
        return currentOrder == "asc" ? "ascending" : "descending";
    }

    string GetSortUrl(string sortField)
    {
        var order = currentOrder == "asc" ? "desc" : "asc";
        return Url.Action("ExistingNetworks", new { sortBy = sortField, sortOrder = order, page = currentPage, pageSize = pageSize }) ?? "";
    }

    string GetSortIcon()
    {
        if (currentOrder == "asc")
        {
            return "<svg class='govuk-!-margin-left-1' width='12' height='12' viewBox='0 0 12 12' fill='none' xmlns='http://www.w3.org/2000/svg' aria-hidden='true' focusable='false'><path d='M6 2L10 8H2L6 2Z' fill='currentColor'/></svg>";
        }
        else
        {
            return "<svg class='govuk-!-margin-left-1' width='12' height='12' viewBox='0 0 12 12' fill='none' xmlns='http://www.w3.org/2000/svg' aria-hidden='true' focusable='false'><path d='M6 10L2 4H10L6 10Z' fill='currentColor'/></svg>";
        }
    }
}

<style>
    [aria-sort] button,
    [aria-sort] button:hover {
        display: inline-flex;
        position: relative;
        margin: 0;
        padding: 0;
        border-width: 0;
        color: #005ea5;
        background-color: transparent;
        box-shadow: 0 0 0 0;
        font-family: inherit;
        font-size: inherit;
        font-size: 1em;
        font-weight: inherit;
        text-align: inherit;
        cursor: pointer;
        align-items: center;
    }

        [aria-sort] button:focus {
            outline: none;
            color: #0b0c0c;
            background-color: #ffdd00;
            box-shadow: 0 -2px #ffdd00, 0 4px #0b0c0c;
        }
</style>

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">

        <h1 class="govuk-heading-l">Heat networks</h1>

        <table class="govuk-table">

            <thead class="govuk-table__head">
                <tr class="govuk-table__row">
                    <th scope="col" class="govuk-table__header" aria-sort="@GetSortDirection()">                        
                        <button data-index="0" onclick="window.location.href='@GetSortUrl("OfgemImportedDate")'" type="button" class="" data-module="govuk-button">
                            Ofgem imported date @Html.Raw(GetSortIcon())
                        </button>
                    </th>
                    <th scope="col" class="govuk-table__header" aria-sort="@GetSortDirection()">                        
                        <button data-index="0" onclick="window.location.href='@GetSortUrl("hnId")'" type="button" class="" data-module="govuk-button">
                            Heat network @Html.Raw(GetSortIcon())
                        </button>
                    </th>                    
                    <th scope="col" class="govuk-table__header">Action</th>                    
                </tr>
            </thead>

            <tbody class="govuk-table__body">
                @if (Model.Items != null && Model.Items.Any())
                {
                    @foreach (var heatNetwork in Model.Items)
                    {
                        <tr class="govuk-table__row">
                            <td class="govuk-table__cell">
                                @if (heatNetwork.OfgemImportedDate.HasValue)
                                {
                                    <time class="local-time" datetime="@heatNetwork.OfgemImportedDate.Value.ToString("o")">
                                        @heatNetwork.OfgemImportedDate.Value.ToString("dd/MM/yyyy")
                                    </time>
                                }
                                else
                                {
                                    @String.Empty
                                }
                            </td>
                            <td class="govuk-table__cell">
                                @heatNetwork.HnId<br />
                                @heatNetwork.Name<br />
                                @heatNetwork.AdditionalDescription
                            </td> 
                            
                            @if(heatNetwork.HeatNetworkType == NullableOfHeatNetworkType.Unset)
                            {
                                <td class="govuk-table__cell"><a class="govuk-link" href="@Url.Action("ExistingNetworksAction", "UserManagement", new { hnId = heatNetwork.HnId, hnName = heatNetwork.Name, action = "RegisterNetwork" })" aria-describedby="@heatNetwork.HnId">
                                    Register with HNTAS
                                </a></td>                               
                            }
                            else
                            {
                                <td class="govuk-table__cell">
                                    <a class="govuk-link" href="@Url.Action("PrepareToAddNetworkDetails", "HeatNetwork", new { hnId = @heatNetwork.HnId, registrationSource = RegistrationSource.OFGEM })" aria-describedby="@heatNetwork.HnId">
                                        Add Network details
                                </a></td>
                            }                            
                        </tr>
                    }
                }
                else
                {
                    <tr class="govuk-table__row">
                        <td class="govuk-table__cell" colspan="5">No existing network found.</td>
                    </tr>
                }
            </tbody>
        </table>

        @if (totalPages > 1)
        {
            <nav class="govuk-pagination" role="navigation" aria-label="Pagination">

                @if (currentPage > 1)
                {
                    <div class="govuk-pagination__prev">
                        <a class="govuk-link govuk-pagination__link" href="@Url.Action("ExistingNetworks", new { sortBy = currentSort, sortOrder = currentOrder, page = currentPage - 1, pageSize = pageSize })" rel="prev">
                            <svg class="govuk-pagination__icon govuk-pagination__icon--prev" xmlns="http://www.w3.org/2000/svg" height="13" width="15" aria-hidden="true" focusable="false" viewBox="0 0 15 13">
                                <path d="m6.5938-0.0078125-6.7266 6.7266 6.7441 6.4062 1.377-1.449-4.1856-3.9768h12.896v-2h-12.984l4.2931-4.293-1.414-1.414z"></path>
                            </svg>
                            <span class="govuk-pagination__link-title">Previous</span>
                        </a>
                    </div>
                }

                <ul class="govuk-pagination__list">
                    @{
                        int startPage = Math.Max(1, currentPage - 2);
                        int endPage = Math.Min(totalPages, currentPage + 2);

                        for (int i = startPage; i <= endPage; i++)
                        {
                            if (i == currentPage)
                            {
                                <li class="govuk-pagination__item govuk-pagination__item--current">
                                    <a class="govuk-link govuk-pagination__link" href="#" aria-label="Page @i" aria-current="page">@i</a>
                                </li>
                            }
                            else
                            {
                                <li class="govuk-pagination__item">
                                    <a class="govuk-link govuk-pagination__link" href="@Url.Action("ExistingNetworks", new { sortBy = currentSort, sortOrder = currentOrder, page = i, pageSize = pageSize })" aria-label="Page @i">
                                        @i
                                    </a>
                                </li>
                            }
                        }

                    }
                </ul>
                <div class="govuk-pagination__next">
                    @if (currentPage < totalPages)
                    {
                        <a class="govuk-link govuk-pagination__link" href="@Url.Action("ExistingNetworks", new { sortBy = currentSort, sortOrder = currentOrder, page = currentPage + 1, pageSize = pageSize })" rel="next">
                            <span class="govuk-pagination__link-title">Next</span>
                            <svg class="govuk-pagination__icon govuk-pagination__icon--next" xmlns="http://www.w3.org/2000/svg" height="13" width="15" aria-hidden="true" focusable="false" viewBox="0 0 15 13">
                                <path d="m8.107-0.0078125-1.4136 1.414 4.2926 4.293h-12.986v2h12.896l-4.1855 3.9766 1.377 1.4492 6.7441-6.4062-6.7246-6.7266z"></path>
                            </svg>
                        </a>
                    }
                </div>
            </nav>
        }

    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Convert all times to local timezone
            document.querySelectorAll('.local-time').forEach(function(timeElement) {
                const utcDate = new Date(timeElement.getAttribute('datetime'));

                // Format to dd/MM/yyyy HH:mm in local timezone
                const day = String(utcDate.getDate()).padStart(2, '0');
                const month = String(utcDate.getMonth() + 1).padStart(2, '0');
                const year = utcDate.getFullYear();
                const hours = String(utcDate.getHours()).padStart(2, '0');
                const minutes = String(utcDate.getMinutes()).padStart(2, '0');

                timeElement.textContent = `${day}/${month}/${year}`;
            });
        });
    </script>
}