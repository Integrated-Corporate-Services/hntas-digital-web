@using HNTAS.Web.UI.Models
@model HasElementBeenRegisteredViewModel

@{
    ViewData["Title"] = "Check your eligibility to register a heat network";
    var fieldOrder = new[] { nameof(Model.HasElementBeenRegistered) };
    var pageData = new QuestionWithRadiobuttonsModel
    {
        Controller = "HeatNetworkEligibility",
        Action = "HasElementBeenRegistered",
        FieldOrder = fieldOrder,
        Questions = new List<Question>
        {
            new Question
            {
                QId = nameof(Model.HasElementBeenRegistered),
                QText = "Has element been registered & entry point evidence been provided?",
                QDescText = null,
                QHintText = null,
                Options = new List<Option>
                {
                    new Option { OptId = "yes", OptText = "Yes", OptHintText = null },
                    new Option { OptId = "no", OptText = "No", OptHintText = null }
                },
                SeletedOption = Model.HasElementBeenRegistered
            },
        }
    };
}

<div class="govuk-width-container">
    @await Html.PartialAsync("_QuestionWithRadiobuttons", pageData)
</div>